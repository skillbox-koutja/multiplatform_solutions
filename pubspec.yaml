name: multiplatform_solutions
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  popover: ^0.2.8+2
  url_launcher: ^6.1.10
  webview_flutter: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  surf_lint_rules: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/